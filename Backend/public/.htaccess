<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Favicon fallback → logo
    RewriteRule ^favicon\.ico$ images/logo.png [L,R=301]

    # Redirect Trailing Slashes If Not A Folder...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Redirect /pages/nama.html → /nama (permanen)
    RewriteCond %{THE_REQUEST} /pages/([^.]+)\.html [NC]
    RewriteRule ^ /%1 [R=301,L]

    # Clean URL: /nama → /pages/nama.html
    RewriteRule ^(berita|galeri|donasi|benah-kampung|kontak|artikel|donasi-detail)$ pages/$1.html [QSA,L]

    # Send Requests To Front Controller...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
